# NPM Configuration for Remedy Clear

# Note: Current npm audit warnings are in @vercel/node v5.5.15
# These vulnerabilities are in build-time dependencies, not runtime
# Fixing requires downgrading to v2.3.0 (breaking change)
# Consider upgrading when Vercel releases patched v5.x or v6.x
